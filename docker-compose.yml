version: '3'
services:
  web:
    container_name: app
    image: app:v1
    build: 
      context: app
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - ~/.env
  # No need since we already installed MongoDB in EC2
  # mongo:
  #   image: mongo:latest
  #   container_name: mongo
  #   restart: always
  #   ports:
  #     - "27017:27017"
      